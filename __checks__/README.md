# Checkly Tests

Run as `bun run checkly test`
Optionally, run as `bun run checkly test --record` to be able to get detailed debugging view in Checkly UI
Run the tests for a particylar file as `bun run checkly test {filePattern}`
Deploy the tests as `bun run checkly deploy`
